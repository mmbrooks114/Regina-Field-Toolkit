# reginafield.datasets

## load_enriched_master(path)
Load the official master dataset.

## load_royal_primes(path)
Load Royal_Primes.csv.

## load_curvature_field(path)
Load curvature field dataset.

## load_motif_entropy_map(path)
Load motif+entropy dataset.

## load_attractor_zones(path)
Load attractor zones dataset.

## load_hilbert_peaks(path)
Load Hilbert envelope peaks.

## load_anomaly_clusters(path)
Load anomaly cluster map.

## merge_master_with_royal(master, royal)
Attach RoyalIndex + IsRoyal.
