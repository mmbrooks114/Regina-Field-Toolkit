# reginafield.rh_bridge

## compute_pi_x(primes, x_grid)
Compute prime-count function.

## compute_li_x(x_grid)
Compute Li(x).

## compute_regina_cumulatives(master, x_grid)
Compute cumulative curvature, entropy, and (if present) RoyalIndex.
