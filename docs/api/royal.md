# reginafield.royal

## attach_royal_status(master, royal_df)
Attach RoyalIndex + IsRoyal.

## filter_royal(df)
Return only Royal primes.

## estimate_extremal_ray_direction(df)
Compute extremal ray.

## project_onto_extremal_ray(df, direction)
Project points onto ray.
